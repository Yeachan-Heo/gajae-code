### Fixed
- Allow explicitly configured preset proxy routing through automatically discovered OpenCodex, matching upstream wire model IDs while preserving stored GJC selectors and direct-provider fallback preferences.
