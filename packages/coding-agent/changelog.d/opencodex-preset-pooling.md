### Fixed
- Allow explicitly configured preset proxy routing through automatically discovered OpenCodex in activation, the preset picker, and SDK availability queries, matching upstream wire model IDs while preserving stored GJC selectors and direct-provider fallback preferences.
