### Added

- Add TUI-only `/mcp status`, `suspend`, `resume`, and `reconnect` controls for root interactive sessions started with an explicit `--mcp-config`, with transactional connection replacement and session-local suppression.
