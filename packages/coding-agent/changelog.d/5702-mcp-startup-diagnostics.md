### Fixed

- Explain the ordinary MCP startup wait and per-server `--timeout` remedy in `gjc mcp list`, and log the server name and actionable reason when startup disconnects an unfinished registration.
