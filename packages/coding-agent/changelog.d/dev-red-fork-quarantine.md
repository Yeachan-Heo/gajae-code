### Fixed

- The utility quarantine contract records `/fork` as a restored first-class TUI-only built-in, matching the shipped `/fork` command from #5515, and still rejects a headless or standalone utility surface for it.
