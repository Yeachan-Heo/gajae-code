### Added

- Add a project-local Bash pre-tool hook example that invokes HOL Guard directly and blocks execution unless Guard classifies the command as explicitly benign with an `allow` minimum action.
